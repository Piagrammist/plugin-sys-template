<?php

use Piagrammist\PluginSys\Utils\ParamsWrapper;

// return function (array $update, ParamsWrapper $args, callable $respond) {
//     if (\preg_match('~^\W<COMMAND>$~i', $args->text, $match)) {
//         yield $respond('Hello, World!');
//     }
// };
